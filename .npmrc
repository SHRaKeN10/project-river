engine-strict=false
auto-install-peers=true
dedupe-peer-dependents=true
# Metro (React Native / Expo) needs a flat node_modules to resolve transitive
# runtime deps like @babel/runtime. This is Expo's recommended setting for
# pnpm monorepos.
node-linker=hoisted
